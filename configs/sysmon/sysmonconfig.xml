<!--
  Sysmon Configuration for Enterprise SOC Detection Lab v2
  Based on SwiftOnSecurity sysmon-config with lab-specific additions
  https://github.com/SwiftOnSecurity/sysmon-config

  Install: sysmon.exe -accepteula -i sysmonconfig.xml
  Update:  sysmon.exe -c sysmonconfig.xml
-->
<Sysmon schemaversion="4.90">
  <CheckRevocation/>
  <EventFiltering>

    <!-- Event ID 1 — Process Creation: Log all, exclude high-volume noise -->
    <RuleGroup name="" groupRelation="or">
      <ProcessCreate onmatch="exclude">
        <Image condition="is">C:\Windows\System32\conhost.exe</Image>
        <CommandLine condition="contains">\\pipe\\</CommandLine>
      </ProcessCreate>
    </RuleGroup>

    <!-- Event ID 3 — Network Connections: Log non-loopback, exclude high-volume -->
    <RuleGroup name="" groupRelation="or">
      <NetworkConnect onmatch="exclude">
        <DestinationIp condition="is">127.0.0.1</DestinationIp>
        <DestinationIp condition="is">::1</DestinationIp>
        <Image condition="is">C:\Windows\System32\svchost.exe</Image>
        <DestinationPort condition="is">53</DestinationPort>
      </NetworkConnect>
    </RuleGroup>

    <!-- Event ID 7 — Image Load: Log unsigned DLL loads -->
    <RuleGroup name="" groupRelation="or">
      <ImageLoad onmatch="include">
        <Signed condition="is">false</Signed>
      </ImageLoad>
    </RuleGroup>

    <!-- Event ID 8 — CreateRemoteThread: Log all (critical for injection detection) -->
    <RuleGroup name="" groupRelation="or">
      <CreateRemoteThread onmatch="exclude">
        <SourceImage condition="is">C:\Windows\System32\csrss.exe</SourceImage>
      </CreateRemoteThread>
    </RuleGroup>

    <!-- Event ID 10 — ProcessAccess: Log LSASS access and browser targeting -->
    <RuleGroup name="" groupRelation="or">
      <ProcessAccess onmatch="include">
        <TargetImage condition="end with">lsass.exe</TargetImage>
        <TargetImage condition="end with">winlogon.exe</TargetImage>
      </ProcessAccess>
    </RuleGroup>

    <!-- Event ID 11 — FileCreate: Log temp/appdata/startup locations -->
    <RuleGroup name="" groupRelation="or">
      <FileCreate onmatch="include">
        <TargetFilename condition="contains">\Temp\</TargetFilename>
        <TargetFilename condition="contains">\AppData\</TargetFilename>
        <TargetFilename condition="contains">\Startup\</TargetFilename>
      </FileCreate>
    </RuleGroup>

    <!-- Event ID 12/13/14 — Registry: Monitor Run keys and common persistence paths -->
    <RuleGroup name="" groupRelation="or">
      <RegistryEvent onmatch="include">
        <TargetObject condition="contains">CurrentVersion\Run</TargetObject>
        <TargetObject condition="contains">CurrentVersion\RunOnce</TargetObject>
        <TargetObject condition="contains">CurrentVersion\RunServices</TargetObject>
        <TargetObject condition="contains">\Winlogon\</TargetObject>
        <TargetObject condition="contains">\Image File Execution Options\</TargetObject>
        <TargetObject condition="contains">\AppCertDlls\</TargetObject>
      </RegistryEvent>
    </RuleGroup>

    <!-- Event ID 22 — DNS Queries: Log all non-system queries -->
    <RuleGroup name="" groupRelation="or">
      <DnsQuery onmatch="exclude">
        <Image condition="is">C:\Windows\System32\svchost.exe</Image>
      </DnsQuery>
    </RuleGroup>

  </EventFiltering>
</Sysmon>
